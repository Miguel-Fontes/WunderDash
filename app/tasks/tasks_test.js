'use strict';

describe('app.tasks module', function() {

  beforeEach(module('app.tasks'));

  describe('tasks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('TasksCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
